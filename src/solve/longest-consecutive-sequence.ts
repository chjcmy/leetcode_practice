/**
 * 128. Longest Consecutive Sequence (Medium)
 * 
 * Given an unsorted array of integers nums, return the length of the longest consecutive elements sequence.
 * You must write an algorithm that runs in O(n) time.
 * 
 * Example:
 * Input: nums = [100,4,200,1,3,2]
 * Output: 6
 * Explanation: The longest consecutive elements sequence is [1, 2, 3, 4, 5, 6]. Therefore its length is 6.
 */

/**
 * 128. 가장 긴 연속 시퀀스 (Medium)
 *
 * 정렬되지 않은 정수 배열 nums가 주어지면, 가장 긴 연속 요소 시퀀스의 길이를 반환합니다.
 * O(n) 시간에 실행되는 알고리즘을 작성해야 합니다.
 *
 * 예시:
 * 입력: nums = [100,4,200,1,3,2]
 * 출력: 6
 * 설명: 가장 긴 연속 요소 시퀀스는 [1, 2, 3, 4, 5, 6]입니다. 따라서 길이는 6입니다.
 */

function longestConsecutive(nums: number[]): number {
    // 여기에 구현하세요
    return 0;
}

// Test cases
console.log(longestConsecutive([100,4,200,1,3,2])); // Expected: 6
console.log(longestConsecutive([0,3,7,2,5,8,4,6,0,1])); // Expected: 9