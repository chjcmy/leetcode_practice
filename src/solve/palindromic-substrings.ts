/**
 * 647. Palindromic Substrings (Medium)
 * 
 * Given a string s, return the number of palindromic substrings in it.
 * A string is a palindrome when it reads the same backward as forward.
 * A substring is a contiguous sequence of characters within the string.
 * 
 * Example:
 * Input: s = "abc"
 * Output: 3
 * Explanation: Three palindromic strings: "a", "b", "c".
 */

/**
 * 647. 회문 부분 문자열 (Medium)
 *
 * 문자열 s가 주어지면 그 안에 있는 회문 부분 문자열의 수를 반환합니다.
 * 문자열은 거꾸로 읽어도 똑같을 때 회문입니다.
 * 부분 문자열은 문자열 내의 연속적인 문자 시퀀스입니다.
 *
 * 예시:
 * 입력: s = "abc"
 * 출력: 3
 * 설명: 세 개의 회문 문자열: "a", "b", "c".
 */

function countSubstrings(s: string): number {
    // 여기에 구현하세요
    return 0;
}

// Test cases
console.log(countSubstrings("abc")); // Expected: 3
console.log(countSubstrings("aaa")); // Expected: 6