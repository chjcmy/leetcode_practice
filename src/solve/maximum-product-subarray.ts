/**
 * 152. Maximum Product Subarray (Medium)
 * 
 * Given an integer array nums, find a contiguous non-empty subarray within the array that has the largest product, and return the product.
 * The test cases are generated so that the answer will fit in a 32-bit integer.
 * A subarray is a contiguous subsequence of the array.
 * 
 * Example:
 * Input: nums = [2,3,-2,4]
 * Output: 6
 * Explanation: [2,3] has the largest product 6.
 */

/**
 * 152. 최대 곱 부분 배열 (Medium)
 *
 * 정수 배열 nums가 주어지면, 가장 큰 곱을 갖는 배열 내의 연속적인 비어 있지 않은 부분 배열을 찾아 곱을 반환합니다.
 * 테스트 케이스는 답이 32비트 정수에 맞도록 생성됩니다.
 * 부분 배열은 배열의 연속적인 하위 시퀀스입니다.
 *
 * 예시:
 * 입력: nums = [2,3,-2,4]
 * 출력: 6
 * 설명: [2,3]이 가장 큰 곱 6을 가집니다.
 */

function maxProduct(nums: number[]): number {
    // 여기에 구현하세요
    return 0;
}

// Test cases
console.log(maxProduct([2,3,-2,4])); // Expected: 6
console.log(maxProduct([-2,0,-1])); // Expected: 0