/**
 * 137. Single Number II (Medium)
 * 
 * Given an integer array nums where every element appears three times except for one, which appears exactly once. Find the single element and return it.
 * You must implement a solution with a linear runtime complexity and use only constant extra space.
 * 
 * Example:
 * Input: nums = [2,2,3,2]
 * Output: 3
 */

/**
 * 137. 단일 숫자 II (Medium)
 *
 * 모든 요소가 세 번 나타나는 정수 배열 nums에서 정확히 한 번 나타나는 단일 요소를 찾아서 반환합니다.
 * 선형 런타임 복잡도를 가진 솔루션을 구현하고 상수 추가 공간만 사용해야 합니다.
 *
 * 예시:
 * 입력: nums = [2,2,3,2]
 * 출력: 3
 */

function singleNumberII(nums: number[]): number {
    // 여기에 구현하세요
    return 0;
}

// Test cases
console.log(singleNumberII([2,2,3,2])); // Expected: 3
console.log(singleNumberII([0,1,0,1,0,1,99])); // Expected: 99