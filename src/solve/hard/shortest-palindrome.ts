/**
 * 214. Shortest Palindrome (Hard)
 * 
 * You are given a string s. You can convert s to a palindrome by adding characters in front of it.
 * Return the shortest palindrome you can find by performing this transformation.
 * 
 * Example:
 * Input: s = "aacecaaa"
 * Output: "aaacecaaa"
 * 
 * Example:
 * Input: s = "abcd"
 * Output: "dcbabcd"
 */

/**
 * 214. 가장 짧은 회문 (Hard)
 *
 * 문자열 s가 주어집니다. s 앞에 문자를 추가하여 s를 회문으로 변환할 수 있습니다.
 * 이 변환을 수행하여 찾을 수 있는 가장 짧은 회문을 반환합니다.
 *
 * 예시:
 * 입력: s = "aacecaaa"
 * 출력: "aaacecaaa"
 *
 * 예시:
 * 입력: s = "abcd"
 * 출력: "dcbabcd"
 */

function shortestPalindrome(s: string): string {
    // 여기에 구현하세요
    return "";
}

// Test cases
console.log(shortestPalindrome("aacecaaa")); // Expected: "aaacecaaa"
console.log(shortestPalindrome("abcd")); // Expected: "dcbabcd"