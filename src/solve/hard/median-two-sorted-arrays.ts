/**
 * 4. Median of Two Sorted Arrays (Hard)
 * 
 * Given two sorted arrays nums1 and nums2 of size m and n respectively, return the median of the two sorted arrays.
 * The overall run time complexity should be O(log (m+n)).
 * 
 * Example:
 * Input: nums1 = [1,3], nums2 = [2]
 * Output: 2.00000
 * Explanation: merged array = [1,2,3] and median is 2.
 */

/**
 * 4. 두 정렬된 배열의 중앙값 (Hard)
 *
 * 각각 크기가 m과 n인 두 개의 정렬된 배열 nums1과 nums2가 주어지면 두 정렬된 배열의 중앙값을 반환합니다.
 * 전체 실행 시간 복잡도는 O(log (m+n))이어야 합니다.
 *
 * 예시:
 * 입력: nums1 = [1,3], nums2 = [2]
 * 출력: 2.00000
 * 설명: 병합된 배열 = [1,2,3]이고 중앙값은 2입니다.
 */

function findMedianSortedArrays(nums1: number[], nums2: number[]): number {
    // 여기에 구현하세요
    return 0;
}

// Test cases
console.log(findMedianSortedArrays([1,3], [2])); // Expected: 2.0
console.log(findMedianSortedArrays([1,2], [3,4])); // Expected: 2.5