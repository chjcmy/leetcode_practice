/**
 * 1044. Longest Duplicate Substring (Hard)
 * 
 * Given a string s, consider all duplicated substrings: (contiguous) substrings of s that occur 2 or more times. The occurrences may overlap.
 * Return any duplicated substring that has the longest possible length. If s does not have a duplicated substring, the answer is "".
 * 
 * Example:
 * Input: s = "banana"
 * Output: "ana"
 * Explanation: "ana" appears at indices 1 and 3.
 * 
 * Example:
 * Input: s = "abcd"
 * Output: ""
 */

/**
 * 1044. 가장 긴 중복된 부분 문자열 (Hard)
 *
 * 문자열 s가 주어지면, 모든 중복된 부분 문자열(s에서 2번 이상 나타나는 (연속적인) 부분 문자열)을 고려합니다. 발생은 겹칠 수 있습니다.
 * 가능한 가장 긴 길이를 가진 중복된 부분 문자열을 반환하세요. s에 중복된 부분 문자열이 없으면 답은 ""입니다.
 *
 * 예시:
 * 입력: s = "banana"
 * 출력: "ana"
 * 설명: "ana"는 인덱스 1과 3에 나타납니다.
 *
 * 예시:
 * 입력: s = "abcd"
 * 출력: ""
 */

function longestDupSubstring(s: string): string {
    // 여기에 구현하세요
    return "";
}

// Test cases
console.log(longestDupSubstring("banana")); // Expected: "ana"
console.log(longestDupSubstring("abcd")); // Expected: ""