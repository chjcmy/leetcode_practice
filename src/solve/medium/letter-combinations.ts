/**
 * 17. Letter Combinations of a Phone Number (Medium)
 * 
 * Given a string containing digits from 2-9 inclusive, return all possible letter combinations that the number could represent.
 * Return the answer in any order. A mapping of digit to letters (just like on the telephone buttons) is given below.
 * Note that 1 does not map to any letters.
 * 
 * Example:
 * Input: digits = "23"
 * Output: ["ad","ae","af","bd","be","bf","cd","ce","cf"]
 */

/**
 * 17. 전화번호의 문자 조합 (Medium)
 *
 * 2-9를 포함하는 숫자가 포함된 문자열이 주어지면, 해당 숫자가 나타낼 수 있는 모든 가능한 문자 조합을 반환합니다.
 * 답은 어떤 순서로든 반환할 수 있습니다. 숫자를 문자로 매핑하는 것은 아래에 나와 있습니다(전화 버튼과 동일).
 * 1은 어떤 문자에도 매핑되지 않습니다.
 *
 * 예시:
 * 입력: digits = "23"
 * 출력: ["ad","ae","af","bd","be","bf","cd","ce","cf"]
 */

function letterCombinations(digits: string): string[] {
    // 여기에 구현하세요
    return [];
}

// Test cases
console.log(letterCombinations("23")); // Expected: ["ad","ae","af","bd","be","bf","cd","ce","cf"]
console.log(letterCombinations("")); // Expected: []
console.log(letterCombinations("2")); // Expected: ["a","b","c"]