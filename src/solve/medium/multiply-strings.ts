/**
 * 43. Multiply Strings (Medium)
 * 
 * Given two non-negative integers num1 and num2 represented as strings, return the product of num1 and num2, also represented as a string.
 * Note: You must not use any built-in BigInteger library or convert the inputs to integer directly.
 * 
 * Example:
 * Input: num1 = "2", num2 = "3"
 * Output: "6"
 */

/**
 * 43. 문자열 곱하기 (Medium)
 *
 * 문자열로 표현된 두 개의 음이 아닌 정수 num1과 num2가 주어지면, num1과 num2의 곱을 문자열로도 표현하여 반환합니다.
 * 참고: 내장 BigInteger 라이브러리를 사용하거나 입력을 직접 정수로 변환해서는 안 됩니다.
 *
 * 예시:
 * 입력: num1 = "2", num2 = "3"
 * 출력: "6"
 */

function multiply(num1: string, num2: string): string {
    // 여기에 구현하세요
    return "";
}

// Test cases
console.log(multiply("2", "3")); // Expected: "6"
console.log(multiply("123", "456")); // Expected: "56088"