/**
 * 29. Divide Two Integers (Medium)
 * 
 * Given two integers dividend and divisor, divide two integers without using multiplication, division, and mod operator.
 * The integer division should truncate toward zero, which means losing its fractional part.
 * Return the quotient after dividing dividend by divisor.
 * 
 * Example:
 * Input: dividend = 10, divisor = 3
 * Output: 3
 * Explanation: 10/3 = 3.33333.. which is truncated to 3.
 */

/**
 * 29. 두 정수 나누기 (Medium)
 *
 * 두 정수 dividend와 divisor가 주어지면 곱셈, 나눗셈 및 나머지 연산자를 사용하지 않고 두 정수를 나눕니다.
 * 정수 나눗셈은 0을 향해 버림해야 합니다. 즉, 소수 부분을 잃게 됩니다.
 * dividend를 divisor로 나눈 후 몫을 반환합니다.
 *
 * 예시:
 * 입력: dividend = 10, divisor = 3
 * 출력: 3
 * 설명: 10/3 = 3.33333.. 이며 3으로 잘립니다.
 */

function divide(dividend: number, divisor: number): number {
    // 여기에 구현하세요
    return 0;
}

// Test cases
console.log(divide(10, 3)); // Expected: 3
console.log(divide(7, -3)); // Expected: -2