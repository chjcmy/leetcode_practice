/**
 * 287. Find the Duplicate Number (Medium)
 * 
 * Given an array of integers nums containing n + 1 integers where each integer is in the range [1, n] inclusive.
 * There is only one repeated number in nums, return this repeated number.
 * You must solve the problem without modifying the array nums and uses only constant extra space.
 * 
 * Example:
 * Input: nums = [1,3,4,2,2]
 * Output: 2
 */

/**
 * 287. 중복된 숫자 찾기 (Medium)
 *
 * n + 1개의 정수를 포함하는 정수 배열 nums가 주어지며, 각 정수는 [1, n] 범위에 포함됩니다.
 * nums에는 반복되는 숫자가 하나만 있으며, 이 반복되는 숫자를 반환합니다.
 * 배열 nums를 수정하지 않고 상수 추가 공간만 사용하여 문제를 해결해야 합니다.
 *
 * 예시:
 * 입력: nums = [1,3,4,2,2]
 * 출력: 2
 */

function findDuplicate(nums: number[]): number {
    // 여기에 구현하세요
    return 0;
}

// Test cases
console.log(findDuplicate([1,3,4,2,2])); // Expected: 2
console.log(findDuplicate([3,1,3,4,2])); // Expected: 3