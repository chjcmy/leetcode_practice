/**
 * 12. Integer to Roman (Medium)
 * 
 * Roman numerals are represented by seven different symbols: I, V, X, L, C, D and M.
 * Given an integer, convert it to a roman numeral.
 * 
 * Example:
 * Input: num = 3
 * Output: "III"
 */

/**
 * 12. 정수를 로마 숫자로 (Medium)
 *
 * 로마 숫자는 I, V, X, L, C, D, M의 7가지 다른 기호로 나타냅니다.
 * 정수가 주어지면 로마 숫자로 변환합니다.
 *
 * 예시:
 * 입력: num = 3
 * 출력: "III"
 */

function intToRoman(num: number): string {
    // 여기에 구현하세요
    return "";
}

// Test cases
console.log(intToRoman(3)); // Expected: "III"
console.log(intToRoman(58)); // Expected: "LVIII"
console.log(intToRoman(1994)); // Expected: "MCMXCIV"