/**
 * 416. Partition Equal Subset Sum (Medium)
 * 
 * Given a non-empty array nums containing only positive integers, find if the array can be partitioned into two subsets such that the sum of elements in both subsets is equal.
 * 
 * Example:
 * Input: nums = [1,5,11,5]
 * Output: true
 * Explanation: The array can be partitioned as [1, 5, 5] and [11].
 */

/**
 * 416. 동일한 부분 집합 합으로 분할 (Medium)
 *
 * 양의 정수만 포함하는 비어 있지 않은 배열 nums가 주어지면, 두 부분 집합의 요소 합이 같도록 배열을 두 부분 집합으로 분할할 수 있는지 확인합니다.
 *
 * 예시:
 * 입력: nums = [1,5,11,5]
 * 출력: true
 * 설명: 배열은 [1, 5, 5]와 [11]로 분할할 수 있습니다.
 */

function canPartition(nums: number[]): boolean {
    // 여기에 구현하세요
    return false;
}

// Test cases
console.log(canPartition([1,5,11,5])); // Expected: true
console.log(canPartition([1,2,3,5])); // Expected: false