/**
 * 42. Trapping Rain Water (Hard) - Different version
 * 
 * Given n non-negative integers representing an elevation map where the width of each bar is 1, compute how much water it can trap after raining.
 * 
 * Example:
 * Input: height = [0,1,0,2,1,0,1,3,2,1,2,1]
 * Output: 6
 */

/**
 * 42. 빗물 가두기 (Hard) - 다른 버전
 *
 * 각 막대의 너비가 1인 고도 맵을 나타내는 n개의 음이 아닌 정수가 주어지면, 비가 온 후 가둘 수 있는 물의 양을 계산합니다.
 *
 * 예시:
 * 입력: height = [0,1,0,2,1,0,1,3,2,1,2,1]
 * 출력: 6
 */

function trapRainWater(height: number[]): number {
    // 여기에 구현하세요
    return 0;
}

// Test cases
console.log(trapRainWater([0,1,0,2,1,0,1,3,2,1,2,1])); // Expected: 6
console.log(trapRainWater([4,2,0,3,2,5])); // Expected: 9