/**
 * 26. Remove Duplicates from Sorted Array (Easy)
 * 
 * Given an integer array nums sorted in non-decreasing order, remove the duplicates in-place such that each unique element appears only once. The relative order of the elements should be kept the same. Then return the number of unique elements in nums.
 * 
 * Example:
 * Input: nums = [1,1,2]
 * Output: 2, nums = [1,2,_]
 * Explanation: Your function should return k = 2, with the first two elements of nums being 1 and 2 respectively.
 */

/**
 * 26. 정렬된 배열에서 중복 제거 (Easy)
 *
 * 감소하지 않는 순서로 정렬된 정수 배열 nums가 주어지면, 각 고유 요소가 한 번만 나타나도록 중복을 제자리에서 제거합니다. 요소의 상대적 순서는 동일하게 유지되어야 합니다. 그런 다음 nums의 고유 요소 수를 반환합니다.
 *
 * 예시:
 * 입력: nums = [1,1,2]
 * 출력: 2, nums = [1,2,_]
 * 설명: 함수는 k = 2를 반환해야 하며, nums의 처음 두 요소는 각각 1과 2입니다.
 */

function removeDuplicates(nums: number[]): number {
    // 여기에 구현하세요
    return 0;
}

// Test cases
console.log(removeDuplicates([1,1,2])); // Expected: 2
console.log(removeDuplicates([0,0,1,1,1,2,2,3,3,4])); // Expected: 5