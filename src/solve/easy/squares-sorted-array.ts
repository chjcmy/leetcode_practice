/**
 * 977. Squares of a Sorted Array (Easy)
 * 
 * Given an integer array nums sorted in non-decreasing order, return an array of the squares of each number sorted in non-decreasing order.
 * 
 * Example:
 * Input: nums = [-4,-1,0,3,10]
 * Output: [0,1,9,16,100]
 * Explanation: After squaring, the array becomes [16,1,0,9,100].
 * After sorting, it becomes [0,1,9,16,100].
 */

/**
 * 977. 정렬된 배열의 제곱 (Easy)
 *
 * 감소하지 않는 순서로 정렬된 정수 배열 nums가 주어지면, 각 숫자의 제곱으로 구성된 배열을 감소하지 않는 순서로 정렬하여 반환합니다.
 *
 * 예시:
 * 입력: nums = [-4,-1,0,3,10]
 * 출력: [0,1,9,16,100]
 * 설명: 제곱한 후 배열은 [16,1,0,9,100]이 됩니다.
 * 정렬한 후에는 [0,1,9,16,100]이 됩니다.
 */

function sortedSquares(nums: number[]): number[] {
    // 여기에 구현하세요
    return [];
}

// Test cases
console.log(sortedSquares([-4,-1,0,3,10])); // Expected: [0,1,9,16,100]
console.log(sortedSquares([-7,-3,2,3,11])); // Expected: [4,9,9,49,121]