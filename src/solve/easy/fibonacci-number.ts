/**
 * 509. Fibonacci Number (Easy)
 * 
 * The Fibonacci numbers, commonly denoted F(n) form a sequence, called the Fibonacci sequence, such that each number is the sum of the two preceding ones, starting from 0 and 1. That is,
 * F(0) = 0, F(1) = 1
 * F(n) = F(n - 1) + F(n - 2), for n > 1.
 * Given n, calculate F(n).
 * 
 * Example:
 * Input: n = 2
 * Output: 1
 * Explanation: F(2) = F(1) + F(0) = 1 + 0 = 1.
 */

/**
 * 509. 피보나치 수 (Easy)
 *
 * 일반적으로 F(n)으로 표시되는 피보나치 수는 0과 1에서 시작하여 각 숫자가 앞의 두 숫자의 합인 피보나치 수열이라는 시퀀스를 형성합니다. 즉,
 * F(0) = 0, F(1) = 1
 * n > 1에 대해 F(n) = F(n - 1) + F(n - 2)입니다.
 * n이 주어지면 F(n)을 계산합니다.
 *
 * 예시:
 * 입력: n = 2
 * 출력: 1
 * 설명: F(2) = F(1) + F(0) = 1 + 0 = 1.
 */

function fib(n: number): number {
    // 여기에 구현하세요
    return 0;
}

// Test cases
console.log(fib(2)); // Expected: 1
console.log(fib(3)); // Expected: 2
console.log(fib(4)); // Expected: 3