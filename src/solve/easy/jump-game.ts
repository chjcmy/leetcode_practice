/**
 * 55. Jump Game (Medium)
 * 
 * You are given an integer array nums. You are initially positioned at the array's first index, and each element in the array represents your maximum jump length at that position.
 * Return true if you can reach the last index, or false otherwise.
 * 
 * Example:
 * Input: nums = [2,3,1,1,4]
 * Output: true
 * Explanation: Jump 1 step from index 0 to 1, then 3 steps to the last index.
 */

/**
 * 55. 점프 게임 (Medium)
 *
 * 정수 배열 nums가 주어집니다. 당신은 처음에 배열의 첫 번째 인덱스에 위치하며, 배열의 각 요소는 해당 위치에서의 최대 점프 길이를 나타냅니다.
 * 마지막 인덱스에 도달할 수 있으면 true를, 그렇지 않으면 false를 반환합니다.
 *
 * 예시:
 * 입력: nums = [2,3,1,1,4]
 * 출력: true
 * 설명: 인덱스 0에서 1로 1단계 점프한 다음 마지막 인덱스로 3단계 점프합니다.
 */

function canJump(nums: number[]): boolean {
    // 여기에 구현하세요
    return false;
}

// Test cases
console.log(canJump([2,3,1,1,4])); // Expected: true
console.log(canJump([3,2,1,0,4])); // Expected: false