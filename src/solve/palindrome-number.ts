/**
 * 9. Palindrome Number (Easy)
 * 
 * Given an integer x, return true if x is a palindrome, and false otherwise.
 * 
 * Example:
 * Input: x = 121
 * Output: true
 * Explanation: 121 reads as 121 from left to right and from right to left.
 */

/**
 * 9. 회문 숫자 (Easy)
 *
 * 정수 x가 주어지면 x가 회문이면 true를, 그렇지 않으면 false를 반환합니다.
 *
 * 예시:
 * 입력: x = 121
 * 출력: true
 * 설명: 121은 왼쪽에서 오른쪽으로, 오른쪽에서 왼쪽으로 121로 읽힙니다.
 */

function isPalindrome(x: number): boolean {
    // 여기에 구현하세요
    return false;
}

// Test cases
console.log(isPalindrome(121)); // Expected: true
console.log(isPalindrome(-121)); // Expected: false
console.log(isPalindrome(10)); // Expected: false