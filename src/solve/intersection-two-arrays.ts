/**
 * 349. Intersection of Two Arrays (Easy)
 * 
 * Given two integer arrays nums1 and nums2, return an array of their intersection. Each element in the result must be unique and you may return the result in any order.
 * 
 * Example:
 * Input: nums1 = [1,2,2,1], nums2 = [2,2]
 * Output: [2]
 */

/**
 * 349. 두 배열의 교집합 (Easy)
 *
 * 두 정수 배열 nums1과 nums2가 주어지면 교집합 배열을 반환합니다. 결과의 각 요소는 고유해야 하며 결과는 어떤 순서로든 반환할 수 있습니다.
 *
 * 예시:
 * 입력: nums1 = [1,2,2,1], nums2 = [2,2]
 * 출력: [2]
 */

function intersection(nums1: number[], nums2: number[]): number[] {
    // 여기에 구현하세요
    return [];
}

// Test cases
console.log(intersection([1,2,2,1], [2,2])); // Expected: [2]
console.log(intersection([4,9,5], [9,4,9,8,4])); // Expected: [9,4] or [4,9]