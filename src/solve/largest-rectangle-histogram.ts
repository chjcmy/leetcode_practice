/**
 * 84. Largest Rectangle in Histogram (Hard)
 * 
 * Given an array of integers heights representing the histogram's bar height where the width of each bar is 1, return the area of the largest rectangle in the histogram.
 * 
 * Example:
 * Input: heights = [2,1,5,6,2,3]
 * Output: 10
 * Explanation: The above is a histogram where width of each bar is 1.
 * The largest rectangle is shown in the red area, which has an area = 10 units.
 */

/**
 * 84. 히스토그램에서 가장 큰 직사각형 (Hard)
 *
 * 각 막대의 너비가 1인 히스토그램의 막대 높이를 나타내는 정수 배열 heights가 주어지면 히스토그램에서 가장 큰 직사각형의 면적을 반환합니다.
 *
 * 예시:
 * 입력: heights = [2,1,5,6,2,3]
 * 출력: 10
 * 설명: 위는 각 막대의 너비가 1인 히스토그램입니다.
 * 가장 큰 직사각형은 빨간색 영역에 표시되며 면적은 10단위입니다.
 */

function largestRectangleArea(heights: number[]): number {
    // 여기에 구현하세요
    return 0;
}

// Test cases
console.log(largestRectangleArea([2,1,5,6,2,3])); // Expected: 10
console.log(largestRectangleArea([2,4])); // Expected: 4