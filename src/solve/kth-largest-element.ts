/**
 * 215. Kth Largest Element in an Array (Medium)
 * 
 * Given an integer array nums and an integer k, return the kth largest element in the array.
 * Note that it is the kth largest element in the sorted order, not the kth distinct element.
 * Can you solve it without sorting?
 * 
 * Example:
 * Input: nums = [3,2,1,5,6,4], k = 2
 * Output: 5
 */

/**
 * 215. 배열에서 k번째로 큰 요소 (Medium)
 *
 * 정수 배열 nums와 정수 k가 주어지면 배열에서 k번째로 큰 요소를 반환합니다.
 * 정렬된 순서에서 k번째로 큰 요소이며, k번째로 고유한 요소가 아닙니다.
 * 정렬하지 않고 풀 수 있습니까?
 *
 * 예시:
 * 입력: nums = [3,2,1,5,6,4], k = 2
 * 출력: 5
 */

function findKthLargest(nums: number[], k: number): number {
    // 여기에 구현하세요
    return 0;
}

// Test cases
console.log(findKthLargest([3,2,1,5,6,4], 2)); // Expected: 5
console.log(findKthLargest([3,2,3,1,2,4,5,5,6], 4)); // Expected: 4