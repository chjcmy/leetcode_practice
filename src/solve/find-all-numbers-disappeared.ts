/**
 * 448. Find All Numbers Disappeared in an Array (Easy)
 * 
 * Given an array nums of n integers where nums[i] is in the range [1, n], return an array of all the integers in the range [1, n] that do not appear in nums.
 * 
 * Example:
 * Input: nums = [4,3,2,7,8,2,3,1]
 * Output: [5,6]
 */

/**
 * 448. 배열에서 사라진 모든 숫자 찾기 (Easy)
 *
 * nums[i]가 [1, n] 범위에 있는 n개의 정수 배열 nums가 주어지면, nums에 나타나지 않는 [1, n] 범위의 모든 정수 배열을 반환합니다.
 *
 * 예시:
 * 입력: nums = [4,3,2,7,8,2,3,1]
 * 출력: [5,6]
 */

function findDisappearedNumbers(nums: number[]): number[] {
    // 여기에 구현하세요
    return [];
}

// Test cases
console.log(findDisappearedNumbers([4,3,2,7,8,2,3,1])); // Expected: [5,6]
console.log(findDisappearedNumbers([1,1])); // Expected: [2]