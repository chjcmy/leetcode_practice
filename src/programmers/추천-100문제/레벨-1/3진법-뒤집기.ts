/*
 * 프로그래머스 - 3진법 뒤집기
 * https://school.programmers.co.kr/learn/courses/30/lessons/68935
 * Level 1
 *
 * **English Description:**
 * Given a natural number `n`, the task is to convert `n` into its ternary (base-3) representation, reverse the order of its digits, and then convert the reversed ternary number back to a decimal (base-10) number. The function should return this final decimal value.
 *
 * **Korean Description:**
 * 자연수 `n`이 매개변수로 주어지면, `n`을 3진법으로 변환하고, 이 3진법 숫자의 앞뒤를 뒤집은 후, 다시 10진법으로 표현한 수를 반환하는 `solution` 함수를 완성해야 합니다.
 */

function solution(n: number): number {
  let answer = 0;
  // 문제 풀이
  return answer;
}

// 예제 테스트
console.log(`예제 1: ${solution(45)}`); // 7
console.log(`예제 2: ${solution(125)}`); // 229
